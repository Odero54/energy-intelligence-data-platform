# Runtime dependencies for the data pipeline (installed with Airflow constraints).
# dbt-snowflake is installed separately in the Dockerfile to avoid dep conflicts.

# Core data
pandas>=2.1,<3
numpy>=1.26,<3
pyarrow>=15.0

# Geospatial (binary wheels include GDAL — no system GDAL needed)
geopandas>=0.14
rasterio>=1.3
shapely>=2.0
pyogrio>=0.7
pyproj>=3.6
rtree>=1.2

# Snowflake
snowflake-connector-python[pandas]>=3.7

# Google Earth Engine
earthengine-api>=0.1.390

# HTTP / API clients
httpx>=0.27
requests>=2.31

# World Bank open data
wbgapi>=1.0

# Environment
python-dotenv>=1.0

# Misc
scipy>=1.13
openpyxl>=3.1
