# Runtime dependencies installed into the custom Airflow image.
# Mirrors pyproject.toml but excludes dev/notebook tools.

# Core data
pandas>=2.2,<3
numpy>=1.26,<2
pyarrow>=15.0

# Geospatial (binary wheels include GDAL — no system GDAL needed)
geopandas>=0.14
rasterio>=1.3
shapely>=2.0
fiona>=1.9
pyproj>=3.6
rtree>=1.2          # sjoin_nearest spatial index

# Snowflake
snowflake-connector-python[pandas]>=3.7

# dbt
dbt-snowflake>=1.9

# Google Earth Engine
earthengine-api>=0.1.390

# HTTP / API clients
httpx>=0.27
requests>=2.31

# World Bank open data
wbgapi>=1.0

# Environment
python-dotenv>=1.0

# Misc
scipy>=1.13
openpyxl>=3.1
